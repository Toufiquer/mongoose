/**
 * @ code split index.ts to app.ts
 * & npm i --save-dev @type/cors
 * & npm i --save-dev @type/express
 *
 *
 * & write type like =>
 * # app: Application, Express
 * # req: Request
 * # res: Response
 * # next: NextFunction
 * # port | PORT : number | string
 *
 *
 * & middleware =>
 * # app.use(cors())
 * # app.use(express.json())
 * # app.use(express.urlencoded())
 *
 *
 * * How to insert data to mongoDB
 * 1. create instance
 * 2. create Schema
 * 3. create Model
 * 4. create Database Query
 *
 * */
