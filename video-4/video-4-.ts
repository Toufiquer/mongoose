/**
 * * Go to mongoose for details
 * # create an instance
 * # create Schema using interface // Schema type will must be camel cased (String,Number,Boolean)
 * # if I skip  required, unique then it's will default value is false; => required: false, unique: false
 * ! Developer will responsible for error on to create Schema.
 * ! Remember which is required which is unique to your application
 *
 *
 *
 * */
